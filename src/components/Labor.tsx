import { Section } from './Section'
import { formatMoney } from '../utils/money'
export function Labor({hours,rate,onHours,onRate}:{hours:number;rate:number;onHours:(v:number)=>void;onRate:(v:number)=>void}) { return <Section title="👷 Mano de Obra"><div className="grid three"><label>Horas estimadas<input type="number" min="0" value={hours} onChange={e=>onHours(Number(e.target.value))}/></label><label>Costo hora ($)<input type="number" min="0" value={rate} onChange={e=>onRate(Number(e.target.value))}/></label><label>Subtotal<input readOnly value={formatMoney(hours*rate)} /></label></div></Section> }
